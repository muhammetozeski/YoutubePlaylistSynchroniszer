// Project-wide global usings. Logger and HelperFunctions live in the global namespace so
// Log(...) and the helpers are callable from anywhere unqualified.
global using static Logger;
global using static HelperFunctions;
